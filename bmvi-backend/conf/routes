# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /api/weather/city/:city             controllers.Application.weatherForCity(city: String)
GET     /api/weather/coords/:lat/:lon       controllers.Application.weatherForCoords(lat: Double, lon: Double)
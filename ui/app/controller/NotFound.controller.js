sap.ui.define([
    "./BaseController"
], function (BaseController) {
    "use strict";

    /**
     * Constructor for a new NotFound Controller.
     *
     * @class
     * Controller for the NotFound View.
     * @extends bmvi.ui.app.controller.BaseController
     *
     * @constructor
     * @alias bmvi.ui.app.controller.NotFound
     */
    var NotFoundController = BaseController.extend("bmvi.ui.app.controller.NotFound");

    return NotFoundController;
});
